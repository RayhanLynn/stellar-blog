# Luckylotus 博客维护与上传指南

这份文档说明怎样在本地预览、发布 Markdown 文章、维护知识库、更新近期心得、插入图片或 PDF，以及将修改部署到 Vercel。

## 1. 开始前准备

博客项目目录：

```text
D:\stellar-blog
```

打开 PowerShell，进入项目：

```powershell
cd D:\stellar-blog
```

第一次使用时安装依赖：

```powershell
pnpm install
```

启动本地预览：

```powershell
pnpm run dev
```

浏览器打开：

```text
http://localhost:4322/
```

如果 4322 端口已被占用，通常说明博客已经在运行，直接刷新浏览器即可。

## 2. 发布一篇 Markdown 文章

文章统一放在：

```text
D:\stellar-blog\source\_posts
```

新建英文文件名，例如：

```text
my-new-post.md
```

使用下面的模板：

```markdown
---
title: 文章标题
date: '2026-09-20 20:00:00'
layout: post
categories:
  - 学习记录
tags:
  - Markdown
  - 学习
description: 用一两句话介绍文章内容。
cover: /assets/my-post-cover.jpg
banner: /assets/my-post-cover.jpg
---

# 第一部分

这里写正文。

## 1.1 小节标题

这里继续写内容。
```

注意事项：

- `title` 是文章标题。
- `date` 决定发布时间和文章排序。
- `categories` 是分类，建议每篇文章只放一个主要分类。
- `tags` 可以写多个标签。
- `description` 会显示在首页文章卡片上。
- `cover` 是首页卡片图片。
- `banner` 是文章页面顶部图片。
- 一级标题使用 `#`，二级标题使用 `##`，不要在标题里重复手写自动目录序号。
- Markdown 的两个段落之间留一个空行，否则网页可能把它们合并。

## 3. 在文章中插入图片

把图片复制到：

```text
D:\stellar-blog\source\assets
```

推荐使用简短的英文文件名，例如：

```text
network-diagram.png
```

在 Markdown 中插入：

```markdown
![网络结构示意图](/assets/network-diagram.png)
```

需要控制宽度时：

```html
<img src="/assets/network-diagram.png" alt="网络结构示意图" style="max-width:720px;width:100%;height:auto;">
```

更换页面封面时，把文章开头的 `cover` 和 `banner` 改成新的图片地址即可：

```yaml
cover: /assets/new-cover.jpg
banner: /assets/new-cover.jpg
```

## 4. 插入或嵌入 PDF

PDF 文件放在：

```text
D:\stellar-blog\source\assets\pdfs
```

普通下载链接：

```markdown
[下载课程资料](/assets/pdfs/course-notes.pdf)
```

在页面中直接阅读：

```html
<figure class="pdf-embed">
  <iframe src="/assets/pdfs/course-notes.pdf" title="课程资料 PDF" height="720"></iframe>
  <figcaption><a href="/assets/pdfs/course-notes.pdf" target="_blank">在新窗口打开 PDF</a></figcaption>
</figure>
```

文件名建议只使用英文、数字和短横线，避免浏览器地址编码问题。

## 5. 更新近期心得

心得数据只需要修改：

```text
D:\stellar-blog\source\_data\moments.yml
```

新增一条时，把最新内容放在文件最上方：

```yaml
- id: '2026-09-20'
  date: '2026-09-20'
  time: '21:30'
  text: '这里填写心得内容。'
  image: '/assets/moment-2026-09-20.jpg'
  image_alt: '照片内容说明'
```

其中：

- `id` 必须唯一，推荐与日期相同。
- `date` 使用 `年-月-日` 格式。
- `time` 可以删除；删除后只显示日期。
- `text` 是心得正文。
- `image` 可以删除；删除后这条心得只显示文字。
- 图片仍然放在 `source/assets`。

心得页面顶部封面与单条心得配图是分开的。当前顶部封面文件是：

```text
D:\stellar-blog\source\assets\moments-cover.jpg
```

想更换封面时，可以直接用同名图片覆盖它；如果要改文件名，则同时修改
`tools/sync-moments.cjs` 中的 `banner: /assets/moments-cover.jpg`。单条心得里的照片只由
`source/_data/moments.yml` 的 `image` 字段控制。

修改后执行：

```powershell
pnpm run sync:moments
```

这个命令会同时更新心得页面和首页右侧的近期心得。

## 6. 修改现有知识库章节

知识库正文位于：

```text
D:\stellar-blog\source\wiki
```

当前主要目录：

```text
source\wiki\networks                    计算机网络
source\wiki\computer-organization       计算机组成与结构
source\wiki\xi-thought-question-bank    习思想题库
```

直接打开对应的 `.md` 文件修改即可。知识库章节目录配置位于：

```text
D:\stellar-blog\source\_data\wiki
```

新增章节时，需要完成两步：

1. 在对应知识库目录中新增 Markdown 文件。
2. 在对应的 YAML 配置文件 `tree` 下添加该文件名，文件名不带 `.md`。

例如新增：

```text
source\wiki\networks\08-review.md
```

在 `source/_data/wiki/networks.yml` 中添加：

```yaml
tree:
  第八章 · 复习:
    - 08-review
```

页面文件模板：

```markdown
---
title: 第八章 · 复习
layout: page
wiki: networks
menu_id: wiki
banner: /assets/snow-mountain-lake.jpg
---

# 本章标题

正文内容。
```

## 7. 直接用 Word 文件更新习思想题库

把更新后的 `.doc` 或 `.docx` 文件放进：

```text
C:\Users\hw\Desktop\习思想题库
```

章节文件名请保持与现有文件一致。然后在项目目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File tools\sync-thought-question-bank.ps1
```

脚本会自动完成：

1. 使用本机 Word 在后台读取旧版 `.doc`。
2. 创建临时 `.docx` 副本，不修改桌面上的原文件。
3. 提取中文题目、选项和答案。
4. 为答案生成可展开区域。
5. 更新 18 个知识库章节和章节目录。

如果更换题库封面，把图片放进 `source/assets`，然后修改：

```text
D:\stellar-blog\source\_data\wiki\xi-thought-question-bank.yml
```

其中的：

```yaml
cover: /assets/xi-thought-cover.jpg
```

章节顶部图片则修改各章节开头的：

```yaml
banner: /assets/xi-thought-cover.jpg
```

为了让以后自动导入仍使用新图片，还要同步修改：

```text
D:\stellar-blog\tools\import-thought-question-bank.py
```

文件顶部的：

```python
COVER = "/assets/xi-thought-cover.jpg"
```

## 8. 添加一个全新的知识库

假设新知识库标识为 `operating-system`：

1. 创建正文目录：

```text
source\wiki\operating-system
```

2. 创建知识库首页和章节 Markdown。
3. 创建配置文件：

```text
source\_data\wiki\operating-system.yml
```

配置模板：

```yaml
name: 操作系统
title: 操作系统
description: 按章节整理操作系统课程笔记。
cover: /assets/operating-system-cover.jpg
tags:
  - 计算机基础
path: /wiki/operating-system/
base_dir: wiki/operating-system/
icon: /assets/avatar.jpg
toc: true
tree:
  开始:
    - index
  第一章 · 概述:
    - 01-introduction
```

4. 在 `source/_data/wiki.yml` 中加入：

```yaml
- operating-system
```

## 9. 修改头像、背景和站点文字

头像文件：

```text
source\assets\avatar.jpg
```

全站背景图：

```text
source\assets\snow-mountain-lake.jpg
```

站点名称、副标题、导航和页脚主要在：

```text
_config.stellar.yml
```

视觉颜色、透明度、题库答案框和页面细节在：

```text
source\assets\forest.css
```

修改图片时可以直接覆盖原文件，也可以使用新文件名并更新对应配置。使用新文件名更不容易受到浏览器缓存影响。

## 10. 构建检查

修改完成后运行：

```powershell
pnpm run build
pnpm run check
```

如果两条命令都没有报错，再启动本地预览：

```powershell
pnpm run dev
```

浏览器看不到新图片时，按 `Ctrl + F5` 强制刷新。

## 11. 部署到 Vercel

在 PowerShell 中运行：

```powershell
cd D:\stellar-blog
git status
git add .
git commit -m "更新博客内容"
git push
```

GitHub 收到推送后，Vercel 会自动开始部署。通常等待一到数分钟，再访问：

```text
https://www.luckylotus.top/
```

如果 Vercel 没有更新：

1. 打开 Vercel 项目页面。
2. 进入 `Deployments`。
3. 检查最新部署是否为 `Ready`。
4. 如果部署失败，点开失败记录查看日志。
5. 确认 GitHub 推送的是 `main` 分支。

## 12. 推荐的日常发布流程

```powershell
cd D:\stellar-blog
pnpm run sync:moments
pnpm run build
pnpm run check
git add .
git commit -m "更新文章和学习笔记"
git push
```

只更新普通 Markdown 文章或知识库章节时，可以省略 `pnpm run sync:moments`。更新习思想 Word 题库时，先运行：

```powershell
powershell -ExecutionPolicy Bypass -File tools\sync-thought-question-bank.ps1
```
