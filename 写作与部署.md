# Luckylotus 博客使用、写作与部署手册

这是一份面向日常使用的操作手册。博客采用 Hexo 8、Stellar 1.44、Markdown、KaTeX 和本地霞鹜文楷字体，最终可以部署到 Vercel。网站源文件在 `source` 目录，`public` 是自动生成的网页，不要直接修改 `public`。

## 1. 本地启动

电脑需要安装 Node.js 22 或 24。在项目目录 `D:\stellar-blog` 打开 PowerShell：

```powershell
pnpm install
pnpm run dev
```

浏览器访问 `http://localhost:4322/`。也可以双击项目根目录的 `start.cmd`。修改 Markdown、配置或样式后，开发服务器通常会自动更新页面；若没有更新，重新执行 `pnpm run dev`。

正式构建和检查：

```powershell
pnpm run build
pnpm run check
```

生成的网站位于 `public`。Hexo 官方也将文章源文件放在 `source/_posts`，通过 `generate` 生成静态页面：[Hexo 写作说明](https://hexo.io/docs/writing)、[Hexo 命令说明](https://hexo.io/docs/commands)。

## 2. 常用内容分别在哪里修改

| 想修改的内容 | 文件或目录 | 操作 |
|---|---|---|
| 网站名称、描述、作者、正式域名 | `_config.yml` | 修改 `title`、`subtitle`、`description`、`author`、`url` |
| 左上角名称和副标题 | `_config.stellar.yml` | 修改 `logo.title` 和 `logo.subtitle` |
| 左上角头像 | `source/assets/avatar.jpg` | 用新的方形 JPG 覆盖同名文件；若更换文件名，还要同步修改头像引用 |
| 全站背景 | `source/assets/snow-mountain-lake.jpg` | 用相同比例的新图片覆盖同名文件 |
| 首页顶部文章封面 | `source/assets/mountains.jpg` | 覆盖图片，或者修改文章中的 `cover` |
| 页面顶部横幅照片 | 每个 Markdown 顶部的 `banner` | 改为 `/assets/你的图片.jpg` |
| 知识库首页的课程封面 | `source/assets/wiki-network-cover.jpg`、`source/assets/wiki-computer-organization-cover.jpg` | 用新的横向图片覆盖对应文件 |
| 首页主卡片标题和介绍 | `source/_posts/welcome.md` | 修改 `title`、`description` 和正文 |
| 左侧“博客指南”文字 | `source/_data/widgets.yml` | 修改 `welcome.content` |
| 右侧站点速览、日历 | `source/_data/widgets.yml` | 修改 `toolbox`；日历自动生成 |
| 右侧近期心得时间线 | `source/_data/widgets.yml` | 修改 `journal.content` 中的日期和文字 |
| 近期心得照片卡片 | `source/moments/index.md` | 复制或修改 `moment-card` |
| 关于页面 | `source/about/index.md` | 直接使用 Markdown 编辑 |
| 页脚链接、建站日期 | `_config.stellar.yml` | 修改 `footer`，建站日同时修改两处 `2026-09-16` |
| 湖蓝颜色、透明度、边框 | `source/assets/forest.css` | 主要修改文件顶部变量和对应组件样式 |
| 顶部菜单、左右栏显示内容 | `_config.stellar.yml` | 修改 `menubar` 和 `site_tree` |

### 更换 JPG 或 PNG 头像

当前头像文件是 `source/assets/avatar.jpg`。以后最简单的换法，是把新头像裁成方形后直接覆盖这个同名文件。若改用其他文件名，需要同步修改：

```text
_config.stellar.yml
source/_data/widgets.yml
source/moments/index.md
source/_data/wiki/*.yml
```

### 调整透明度和湖蓝色

打开 `source/assets/forest.css`，最常用的是：

```css
:root {
  --forest-border: rgba(105, 191, 202, .48); /* 最后一个数越大，边框越明显 */
  --forest-glass: rgba(20, 42, 45, .035);    /* 最后一个数越小，侧栏越透明 */
}
```

右侧栏透明度在 `.l_right .widget-wrapper` 的 `background`，文章卡片透明度在 `.post-list .post-card`。颜色主配置位于 `_config.stellar.yml` 的 `style.color`。

### 更换页面顶部照片

把照片放进 `source/assets` 或 `source/assets/notes`，然后修改对应 Markdown 开头的 `banner`：

```yaml
---
title: 关于
layout: page
banner: /assets/notes/about-banner.jpg
---
```

普通文章的 `cover` 控制首页列表封面，`banner` 控制点进文章后的顶部横幅，两者可以使用不同图片。归档、分类和标签属于自动生成的列表页，没有独立 Markdown 横幅。

## 3. 写一篇普通 Markdown 文章

文章放在 `source/_posts`。可以复制现有文章，也可以执行：

```powershell
pnpm run new -- "文章标题"
```

文章顶部使用以下信息：

```markdown
---
title: 我的第一篇笔记
date: 2026-09-17 20:00:00
tags: [学习, 网络]
categories: [计算机学习]
description: 一句话介绍这篇文章。
---

## 第一部分

从这里开始写正文。
```

保存后会自动出现在首页、分类、标签和归档中。

## 4. 导入已有 Markdown

“经验分享”文章来自：

```text
D:\LRH study\LRH learning\06 经验分享
```

修改原文后，可以重新同步文章及其 PDF 附件：

```powershell
pnpm run sync:experience
```

同步配置位于 `tools/sync-experience-posts.cjs`，只读取原始目录，不会修改个人资料。新的经验分享文章需要先在脚本的 `articles` 列表中登记标题、文件名和网址英文名。

导入普通文章：

```powershell
pnpm run import -- "D:\Notes\我的笔记.md"
```

指定标题和网址文件名：

```powershell
pnpm run import -- "D:\Notes\我的笔记.md" --title "我的笔记" --slug my-note
```

导入为计算机网络的章节：

```powershell
pnpm run import -- "D:\Notes\网络层.md" --series networks --chapter "第四章 · 网络层" --slug network-layer
```

导入工具不会覆盖同名文件，会保留 Markdown 正文和公式。图片、PDF 等附件需要另外复制到 `source/assets`。

## 5. 按章节管理课程笔记

当前两个知识库来自下面的个人笔记目录：

```text
D:\LRH study\LRH learning\02 research\03 专业课学习\计算机网络
D:\LRH study\LRH learning\02 research\03 专业课学习\计算机组成与结构
```

修改原笔记或增加文章后，在博客目录执行：

```powershell
pnpm run sync:study
pnpm run build
pnpm run check
```

同步程序位于 `tools/sync-study-wikis.cjs`。它只读取个人笔记，绝不会删除或改写 `D:\LRH study` 中的内容；它会重新生成博客中的两个知识库、复制被引用的本地图片，并清理旧的博客知识库副本。计算机组成与结构的多层文件夹会自动转换为左侧章节分组。旧 Notion 配置、密码等网页不需要的页头字段不会被发布。

如果在个人笔记目录中增加了新的 Markdown 文件，还需要在 `tools/sync-study-wikis.cjs` 对应知识库的 `groups` 数组里登记文件路径、网页英文名和显示标题，然后再次执行同步。

以计算机网络为例：

```text
source/wiki/networks/                  章节 Markdown
source/_data/wiki/networks.yml        章节目录和顺序
```

新建 `source/wiki/networks/network-layer.md`：

```markdown
---
title: 04 · 网络层：分组如何抵达目的地
layout: page
wiki: networks
menu_id: wiki
---

## 网络层概述

正文……
```

然后在 `source/_data/wiki/networks.yml` 的 `tree` 中加入：

```yaml
tree:
  第四章 · 网络层:
    - network-layer
```

这里的名称必须与 Markdown 文件名一致，但不包含 `.md`。分组和排列顺序就是网站左侧章节目录的显示顺序。

### 新建一个完整知识库

例如新建“数据结构”，先创建以下目录和配置文件：

```text
source/wiki/data-structure/
source/_data/wiki/data-structure.yml
```

在 `source/wiki/data-structure/index.md` 中写：

```markdown
---
title: 数据结构
layout: page
wiki: data-structure
menu_id: wiki
banner: /assets/notes/data-structure-banner.jpg
---

# 章节目录

1. [第一章 · 绪论](/wiki/data-structure/introduction/)
```

再新建第一章 `source/wiki/data-structure/introduction.md`：

```markdown
---
title: 第一章 · 绪论
layout: page
wiki: data-structure
menu_id: wiki
banner: /assets/notes/data-structure-banner.jpg
---

从这里开始写正文。
```

最后在 `source/_data/wiki/data-structure.yml` 中登记知识库和章节顺序：

```yaml
name: 数据结构
title: 数据结构
description: 按章节整理数据结构与算法。
tags:
  - 计算机基础
path: /wiki/data-structure/
base_dir: wiki/data-structure/
icon: /assets/avatar.jpg
toc: true
tree:
  开始:
    - index
  第一章 · 绪论:
    - introduction
```

`data-structure` 是知识库的英文标识，三个文件中的 `wiki`、目录名和 YAML 文件名必须一致。以后新增章节，只需增加一个 Markdown 文件，再把不带 `.md` 的文件名加入 `tree`。

## 6. 插入图片

将图片放到 `source/assets/notes`，然后在 Markdown 中写：

```markdown
![网络结构图](/assets/notes/network.png)
```

建议使用英文或数字文件名，避免空格。也可以使用公开 HTTPS 图片地址。Obsidian 的 `![[图片.png]]` 是专有语法，需要改成标准 Markdown 图片语法。

## 7. 插入数学公式

行内公式：

```latex
发送时延为 $t = \frac{L}{R}$。
```

独立公式前后留空行：

```latex
$$
C = B \log_2\left(1 + \frac{S}{N}\right)
$$
```

公式由本地 KaTeX 构建，不依赖网页加载外部公式服务。不要把需要渲染的公式放进代码块。

## 8. 插入 PDF

将 PDF 放到 `source/assets/pdfs`，然后在 Markdown 中写：

```text
{% pdf /assets/pdfs/计算机网络讲义.pdf 720px %}
```

第二个参数是阅读框高度，可以省略，也可以写 `80vh`。页面下方会自动生成“打开或下载 PDF”链接。也支持公开的 HTTPS PDF 地址。

## 9. 更新近期心得

只修改 `source/_data/moments.yml`，然后执行 `pnpm run sync:moments`。脚本会同时生成心得页面和首页右侧时间线。

每条心得的 `id` 是点赞记录使用的永久标识。发布后不要修改 `id`；调整日期、文字或图片时，只修改 `date`、`text`、`image` 等字段。

## 10. 三种上传和发布方式

### 方式 A：本地写作后推送，最推荐

```powershell
git add .
git commit -m "更新博客内容"
git push
```

Vercel 连接 GitHub 后，每次推送都会自动构建和上线。

### 方式 B：在 GitHub 网页上传 Markdown

进入仓库中的 `source/_posts`，选择 **Add file → Upload files**，上传 `.md` 后提交。GitHub 网页单个文件目前限制为 25 MiB，图片或 PDF 较大时建议使用本地 Git；具体步骤见 [GitHub 添加文件文档](https://docs.github.com/en/repositories/working-with-files/managing-files/adding-a-file-to-a-repository)。

章节笔记上传到对应的 `source/wiki/专栏名`，并同时编辑 `source/_data/wiki/专栏名.yml`。图片上传到 `source/assets/notes`，PDF 上传到 `source/assets/pdfs`。

### 方式 C：使用本项目的导入命令

适合将 Typora、Obsidian 或其他目录中的单篇 Markdown 快速复制进博客：

```powershell
pnpm run import -- "D:\Notes\文章.md"
```

导入后先执行 `pnpm run dev` 检查，再提交到 GitHub。

## 11. 第一次部署到 GitHub

先在 GitHub 创建一个空仓库，例如 `stellar-blog`。不要勾选自动创建 README，随后在项目目录执行：

```powershell
git init
git add .
git commit -m "初始化个人博客"
git branch -M main
git remote add origin https://github.com/你的用户名/stellar-blog.git
git push -u origin main
```

把示例地址换成自己的仓库地址。如果 GitHub 要求登录，按照浏览器弹出的授权步骤完成即可。

## 12. 部署到 Vercel

1. 登录 Vercel，选择 **Add New → Project**。
2. 连接 GitHub 并导入刚才的博客仓库。
3. Framework Preset 选择 **Other**。
4. Build Command 填写 `pnpm run build`。
5. Output Directory 填写 `public`。
6. Install Command 保持自动识别；项目包含 `pnpm-lock.yaml`。
7. Node.js 选择 22 或 24。
8. 点击 **Deploy**。

Vercel 会克隆 Git 仓库、安装依赖、执行构建命令，并只发布 Output Directory 中的内容；这些设置也可以在项目的 **Settings → Build and Deployment** 修改：[Vercel 构建设置](https://vercel.com/docs/builds/configure-a-build)、[Git 仓库部署](https://vercel.com/docs/git)。

以后只需：

```powershell
git add .
git commit -m "新增一篇笔记"
git push
```

Vercel 就会自动发布新版本。

## 13. 绑定自己的域名

1. 打开 Vercel 项目，进入 **Settings → Domains**。
2. 输入自己的域名，例如 `blog.example.com`。
3. 按 Vercel 页面给出的 DNS 记录，前往域名服务商添加记录。
4. 等待 Vercel 显示配置成功和证书生效。
5. 修改 `_config.yml` 中的正式地址：

```yaml
url: https://你的正式域名
```

6. 提交并推送这次修改。

DNS 记录以 Vercel 当时显示的内容为准，不要直接照抄网上旧教程里的 IP 地址。

## 14. 发布前检查清单

```powershell
pnpm run build
pnpm run check
```

- 首页、章节目录、上一篇和下一篇能否打开。
- 手机页面是否出现横向滚动。
- 图片和 PDF 是否能访问。
- 数学公式是否正常渲染。
- `_config.yml` 中的 `url` 是否为正式域名。
- 示例文章、示例心得和个人信息是否已经替换。
- `source` 中是否误放了私人笔记、密钥或密码。

访问量由不蒜子提供。本地预览不会计入正式站点数据，会显示 `0`；使用正式域名访问后才会加载对应域名的 PV 和 UV。
